{props.children}

| Value     | Effect                                                                                     |
| --------- | ------------------------------------------------------------------------------------------ |
| `"warn"`  | Default when [`strict: false`](CONFIG_STRICT). Show as warning; does not affect exit code. |
| `"error"` | Default when [`strict: true`](CONFIG_STRICT). Show as error; exits 1.                      |
| `"fix"`   | {props.fixEffect}                                                                          |
