import { Code } from "astro:components";

Syncpack will print colours and clickable hyperlinks to the terminal using [ANSI escape code](HREF_ANSI) unless this option is set.

<Code code={`syncpack ${props.command} --no-ansi`} lang="bash" />
