---
title: formatBugs
---

import ConfigOnly from "@partials/tips/config-only.mdx";

When using the `format` command, enables formatting of the `bugs` field of `package.json` into a shorthand format.

## Default value

```json title=".syncpackrc.json"
{
  "formatBugs": false
}
```

<ConfigOnly />
