---
title: dependencyTypes
---

import DefaultDependencyTypes from '@partials/default-dependency-types.mdx';
import Details from '@site/components/details.astro';

Areas where versions are usually defined can be toggled on and off via this property.

<Details title="Example: Only include `dependencies` and `devDependencies`">

**Only** `dependencies` and `devDependencies` will be inspected:

```json title=".syncpackrc"
{
  "dependencyTypes": ["dev", "prod"]
}
```

</Details>

<Details title="Example: Exclude `dependencies` and `devDependencies`">

Everything **except** `dependencies` and `devDependencies` will be inspected:

```json title=".syncpackrc"
{
  "dependencyTypes": ["!dev", "!prod"]
}
```

</Details>

## Default dependency types

All are enabled by default.

<DefaultDependencyTypes />

:::tip

Learn more about the [`local` dependency type](/syncpack/guide/local-package-versions).

:::
