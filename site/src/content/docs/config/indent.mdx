---
title: indent
description: Configure indentation spacing for formatted package.json files
---

When syncpack writes to package.json files, determines which characters to indent the JSON with.

## Default value

```json title=".syncpackrc.json"
{
  "indent": "  "
}
```
