---
title: IsCatalogDefinition
status: valid
description: Instance is a catalog definition and the source of truth for sibling instances using the catalog protocol
---

## When this happens

- ✓ Instance is a catalog definition in `pnpm-workspace.yaml` or root `package.json`
- ✓ Zero or more siblings use the `catalog:` protocol for this dependency
