import { Code } from 'astro:components';

Override your [specifierTypes](/syncpack/config/specifier-types) configuration to run <code>{`syncpack ${props.command}`}</code> against dependency instances with version specifiers of the given types, such as `exact`, `range`, or `workspace-protocol`.

<Code
  code={`# only include exact versions
syncpack ${props.command} --specs 'exact'
# only include latest and range versions
syncpack ${props.command} --specs 'latest,range'
# exclude unsupported versions
syncpack ${props.command} --specs '!unsupported'`}
  lang="bash"
/>
