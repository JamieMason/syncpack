import { Code } from 'astro:components';

Syncpack uses [cosmiconfig](https://github.com/cosmiconfig/cosmiconfig) to discover your config file, but a custom location can be provided instead.

<Code code={`syncpack ${props.command} --config ./config/.syncpackrc`} lang="bash" />
