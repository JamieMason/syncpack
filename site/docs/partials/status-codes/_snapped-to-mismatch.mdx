import Link from '@docusaurus/Link';

This instance was matched by a <Link to='/config/version-groups/snapped-to'>Snapped To</Link> Version Group, the version it should use is the one set in the first package named in the `snapTo` array of this Version Group which depends on this dependency – but its version is not identical to that.
