import Link from '@docusaurus/Link';

This instance belongs to a <Link to='/config/semver-groups/with-range'>With Range</Link> Semver Group, but the semver range it is using does not match the one defined on the `range` property of that group.
